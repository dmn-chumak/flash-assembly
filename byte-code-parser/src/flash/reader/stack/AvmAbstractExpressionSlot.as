package flash.reader.stack {
    import flash.reader.struct.LinkerReference;

    public class AvmAbstractExpressionSlot extends AvmAbstractSlot {
        public var classLink:LinkerReference;
    }
}
