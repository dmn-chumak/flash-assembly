package flash.reader.bytes {
    import flash.reader.stream.StreamReader;

    public final class AbcClass extends AbcContainer {
        public function AbcClass(reader:StreamReader) {
            super(reader);
        }
    }
}
