package flash.reader.bytes {
    import flash.reader.stream.StreamReader;

    public final class AbcScript extends AbcContainer {
        public function AbcScript(reader:StreamReader) {
            super(reader);
        }
    }
}
